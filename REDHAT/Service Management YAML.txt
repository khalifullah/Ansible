Service Management YAML

---
- name: Manage Services
  hosts: all
  become: yes
  tasks:
    - name: Start and enable a service  # Replace 'service' with your desired service name
      service:
        name: httpd  # Replace 'httpd' with the actual service name
        state: started
        enabled: yes

    - name: Check the status of the service
      service_facts:
